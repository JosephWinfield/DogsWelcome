<h1>
  Dog Friendly Places
</h1>
<ul>
  <% @places.each do |place| %>
    <li>
      <%= link_to place.name, place_path(place) %>
      <%= place.address %>
      <%= place.city %>
      <%= place.state %>
      <%= place.zip %>
    </li>
    <% end %>
</ul>
