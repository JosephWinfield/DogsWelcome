.places-form, .sign-in {
  width: 80%;
  margin: 0 auto;
}

.places-form h1, .new_user_heading {
  text-align: center;
}

.input {
  outline: none;
  background: transparent;
  border: none;
  border-bottom: 1px solid #ececec;
  padding-bottom: 10px;
  font-size: 22px;
  text-align: left;
  width: 100%;
  color: #ececec;
  font-weight: 700;
  font-style: italic;
}

/*.places-form textarea {
  background: transparent;
  width: 100%;
  height: 100px;
  font-family: "adelle-sans";
  color: #ececec;
  font-size: 22px;
}*/

.submit {
  width: 100%;
  height: 30px;
  background: transparent;
  border: none;
  color: #5ed7fa;
  font-size: 22px;
  font-weight: 700;
  font-style: italic;
}

.places-form label, .new_user label {
  display: none;
}

.new-user-form {
  margin: 0 auto;
}
